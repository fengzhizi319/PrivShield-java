{{/*
=============================================================================
安装后提示信息（NOTES.txt）
=============================================================================
helm install 成功后终端打印的信息，用于指导用户下一步操作。
支持 Go 模板语法，可引用 values 和 helper 函数动态生成内容。
=============================================================================
*/}}
PrivShield has been installed.

Release name: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Flavor: {{ .Values.flavor }}

REST service: {{ include "PrivShield.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.restPort }}
gRPC service: {{ include "PrivShield.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.grpcPort }}

{{- if .Values.llm.enabled }}
Layer-3 LLM service: {{ include "PrivShield.fullname" . }}-llm.{{ .Release.Namespace }}.svc:{{ .Values.llm.port }}/v1 (independent vLLM Deployment)
  - LLM 升级/重启不影响 core；LLM 挂掉时 core 自动降级 Layer-1+2
  - 模型权重来源：llm.storage.hostPath（宿主 .models）或 llm.storage.existingClaim（PVC）
  - GPU 调度：llm.nodeSelector / llm.tolerations（需 NVIDIA device plugin）
{{- end }}

{{- if .Values.security.tls.enabled }}
TLS is enabled. Make sure the certificate secret is mounted correctly.
{{- end }}
{{- if .Values.security.auth.enabled }}
Auth is enabled. Make sure the API key secret is configured.
{{- end }}
